/*
 *
 * kcc scanner, the scanner will scan and tokinize the target file, to be passed
 * over to parsing in the from of tokens
 *
 */

#include<stdlib.h>
#include<stdio.h>

int main() {
	/*
	 * 
	 * Temporary main function
	 *
	 */
	return EXIT_SUCCESS;
}
